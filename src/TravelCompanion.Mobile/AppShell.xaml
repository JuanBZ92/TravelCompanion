<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TravelCompanion.Mobile.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:TravelCompanion.Mobile.Pages"
    Title="Travel Companion">

    <TabBar>
        <ShellContent
            Title="Recomendaciones"
            ContentTemplate="{DataTemplate pages:RecommendationsPage}"
            Route="recommendations" />

        <ShellContent
            Title="Mapa"
            ContentTemplate="{DataTemplate pages:MapPage}"
            Route="map" />

        <ShellContent
            Title="Schedule"
            ContentTemplate="{DataTemplate pages:SchedulePage}"
            Route="schedule" />

        <ShellContent
            Title="Paquetes"
            ContentTemplate="{DataTemplate pages:PackagesPage}"
            Route="packages" />

        <ShellContent
            Title="Soporte"
            ContentTemplate="{DataTemplate pages:SupportPage}"
            Route="support" />
    </TabBar>

</Shell>
